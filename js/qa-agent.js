// ============================================================
// qa-agent.js — PLACEHOLDER
// La version complète arrive dans la livraison 3
// ============================================================

console.log("[Orore] qa-agent.js — placeholder, en attente de livraison 3");

// Branche juste le bouton "Questions à Orore" sur un message
document.addEventListener('DOMContentLoaded', () => {
  const btn = document.getElementById('btn-launch-qa');
  if (btn) {
    btn.addEventListener('click', () => {
      alert("Le mode Q&A avec Orore arrive dans la prochaine livraison ✦");
    });
  }
});
